{
  "name": "hybrid_letterbox_app",
  "version": "1.0.0",
  "dependencies": {
    
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-es2015": "^6.6.0",
    "babel-plugin-transform-es2015-modules-amd": "^6.8.0"
  },
  "scripts": {
    "watch": "babel --watch src --out-dir js --source-maps inline --copy-files",
    "build": "babel src --out-dir js --source-maps inline --copy-files" 
  }
}
